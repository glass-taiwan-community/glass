{
  // Baselined pre-existing violations, matching the root eslint.config.mjs approach.
  // Counts at 2026-08-31. These are cosmetic - the rendered output is identical either way -
  // so they are recorded as warnings rather than left as errors that keep `next lint` red.
  "extends": "next/core-web-vitals",
  "rules": {
    "react/no-unescaped-entities": "warn"
  }
}
